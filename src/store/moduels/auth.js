const state = {
    data: [],
    token: 'fake-token'
}
const actions = {
    // eslint-disable-next-line no-unused-vars

}
const mutations = {}
export default {
    namespaced: true,
    state,
    actions,
    mutations
}
